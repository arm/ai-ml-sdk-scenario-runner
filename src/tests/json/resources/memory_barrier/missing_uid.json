{
    "commands": [
    ],
    "resources": [
        {
            "memory_barrier": {
                "src_access": "compute_shader_write",
                "dst_access": "compute_shader_read"
            }
        }
    ]
}
